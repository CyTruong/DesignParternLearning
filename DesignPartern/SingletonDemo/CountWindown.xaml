<Window x:Class="DesignPartern.SingletonDemo.CountWindown"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesignPartern.SingletonDemo"
        mc:Ignorable="d"
        Title="CountWindown" Height="179.204" Width="208.628">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label x:Name="label" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label x:Name="lbCount" Content="Curentnumer : 0"/>
        </Label>
        <Button x:Name="btUpdate" Click="BtUpdate_Click" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="104" Content="Update" Style="{StaticResource MaterialDesignOutlinedButton}" />
        <Button x:Name="btIncrease" Click="BtIncrease_Click" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Width="104" Content="Increase" />
    </Grid>
</Window>
